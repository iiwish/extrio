# 来源执行反馈修复

状态：Needs_Review。用户要求的故障修复及执行弹窗已实施，验证见[执行证据](../reviews/execution-dialog/execution.md)，待用户验收。直接执行，不委派、不发布规则、不调用付费模型、不修改真实任务历史。

## 范围与验证

ED-01：来源浏览器加载超时保持失败语义，不生成 HTTP 0 假状态；仅对明确导航超时进行一次重试，共享原始网络预算、目标主机与 robots 边界。拒绝访问、越界及结构错误不自动重试。浏览器关闭后的路由回调不产生额外错误或污染下一次尝试。范围为 source_clients.py、worker.py 与相关测试。

ED-02：来源详情执行进度使用现有 Dialog。任务开始/接续时默认打开；用户收起后轮询不重新打开、不停止执行；新任务再次默认打开。保留完成/失败状态、阶段记录和任务详情入口，页面只留查看按钮。使用既有字号、颜色与 8px 圆角，宽度不超过 760px，高度不超过视口减 64px，正文独立滚动。覆盖加载、运行、成功、失败、收起与恢复，中文和英文，四个桌面视口。范围为来源详情组件、相关 i18n、CSS、测试与四份产品文档。

先运行失败测试，再实现并验证相关后端/前端回归、pnpm build/lint、桌面视觉 QA、文档清单和 git diff --check。真实来源只做匿名读取验证，不触发模型或真实任务重跑。完成后记录真实证据并进入 Needs_Review。
