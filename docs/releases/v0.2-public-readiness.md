# v0.2 public readiness

This checklist is the release gate for the first public Extrio alpha. A checked item requires
repeatable evidence in the repository or GitHub Actions.

## Product and security

- [x] A new instance requires first-run administrator setup.
- [x] Control-plane endpoints reject unauthenticated requests.
- [x] Passwords use Argon2 and sessions are server-side revocable.
- [x] Login throttling, logout, and secure-cookie behavior have automated coverage.
- [x] Secrets and generated runtime keys are excluded from source and container layers, and CI
  scans tracked source for committed secrets.

## Build and supply chain

- [x] Source, frontend, and backend test suites pass from a clean checkout.
- [x] Docker Compose starts the web, API, and worker services and passes an authenticated smoke test.
- [x] Version-tag release automation builds backend and web images for `linux/amd64` and
  `linux/arm64`.
- [x] Release builds request BuildKit provenance and SBOM attestations.
- [x] Trivy blocks unfixed HIGH or CRITICAL operating-system and library vulnerabilities in
  container verification and release workflows.
- [x] Release automation applies keyless GitHub Actions signatures and provenance attestations to
  published manifests.

## Open-source operations

- [x] Apache-2.0 license, notice, contribution, security, support, governance, and conduct policies are present.
- [x] Maintainers, roadmap, issue templates, pull request template, and CODEOWNERS are present.
- [x] README setup and deployment instructions match the tested release path.
- [x] GitHub repository description, topics, vulnerability alerts, automated security fixes, and
  merge settings are configured.
- [x] The repository remains private until this checklist and the final maintainer review are complete.

## Release evidence

Activation was executed on 2026-09-03. Recorded evidence:

- [x] Push the selected `v*` tag and record the release workflow run.
  - `v0.2.0` pushed 2026-09-03; release run
    https://github.com/iiwish/extrio/actions/runs/33707446933 (success), GitHub Release
    https://github.com/iiwish/extrio/releases/tag/v0.2.0.
- [x] Record backend and web image manifest digests and verify both architecture manifests.
  - `ghcr.io/iiwish/extrio-backend:v0.2.0` = `sha256:5c2a5fdff9b8b1c60fb628479cc743c532c41edd14f9fd4ead85960b6a665636`
  - `ghcr.io/iiwish/extrio-web:v0.2.0` = `sha256:62ca479b76ce31e5ef2e5c2bb65fe8d047d9e8fe7a4e8470837fbf5e07f3f283`
  - `linux/amd64` and `linux/arm64` manifests were built and pushed by the release workflow
    (backend and web build-and-publish steps).
- [x] Verify the published SBOM, provenance, Cosign signatures, and zero-blocking-vulnerability result.
  - Trivy gates (`exit-code: 1`, `ignore-unfixed: true`) passed for both images on both platforms.
  - Cosign keyless signatures are attached to the published digests (registry referrers).
  - Build provenance and SBOM attestations persisted: attestation records
    [44876677](https://github.com/iiwish/extrio/attestations/44876677) (backend) and
    [44876687](https://github.com/iiwish/extrio/attestations/44876687) (web).
- [x] Enable required branch checks after the repository becomes public or the account supports
  private-repository branch protection.
  - Repository switched to public on 2026-09-03; branch protection on `main` requires the
    `repository-security`, `backend`, `web`, and `publish` checks and rejects force pushes
    and branch deletion.
- [x] Record the final maintainer review and change repository visibility only by explicit decision.
  - Maintainer confirmed the personal-identity exposure in git history and authorized the
    visibility change; the repository is public at https://github.com/iiwish/extrio.

Accepted residual risks and deferred work for the v0.3 cycle:

- Dependabot alerts at release time: `nltk` HIGH (no fixed release; model-artifact path
  hardening advisory; not reachable through Extrio runtime paths) and `qs` MEDIUM ×2
  (transitive browser-bundled dependency; server-side DoS vectors not applicable).
- Deferred major dependency bumps: TypeScript 7 and `@types/node` 26 (PRs #6 and #7 closed
  to keep the release branch green; dependabot will re-propose).

Source CI, Docker Compose E2E, and desktop browser QA at `1440x900` and `1280x800` are
recorded for this release: CI and Container verification runs on `4f8d9d4` and `364c98b`,
i18n QA screenshots under `docs/reviews/public-alpha-2026-09-02/`.
