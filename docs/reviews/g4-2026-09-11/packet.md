# G4 集成验收与发布候选

状态：Needs_Review（本地工程）；G4 整体验收 Pending。用户于 2026-09-11 要求在观测无异常时继续完成 G4。P27/P29/P30 与本地候选安装、升级回退均有通过证据；P28 真实模型、P31 完整观测和用户签收未完成。采用已确认的 [交付规划](../../planning/v1.0-delivery-plan.md) P27–P32 和 [1.0 适用范围](../../planning/v1.0-scope-matrix.md)。72 小时尚未结束，不缩短窗口，不把阶段正常当作验收通过。交付见 [验证](validation.md)、[复核](review.md) 与 [候选清单](release-candidate.md)。

## 执行边界

Direct Execute，未授权委派。保留基准 `86ebcc4b570f8b5b5acf3f1c6ba0f70ac7ed42f7` 以来全部 G1/G2/G3 未提交工作。只对本包创建的独立测试库、账号、来源、接收器和进程执行写入/故障演练。无 Git 提交、推送、正式发布、maco 或生产基础设施操作。已有 G2/G3 QA 和 72 小时观察不被清理或冒用。

允许修改：集成/升级/回滚测试与自包含 fixture、验收脚本；有反例支持的后端/前端小范围修复、对应合同/类型/i18n；候选制品清单、当前运行手册、四份主合同及本目录证据。禁止重写产品架构、绕过鉴权/robots/SSRF/签名、模拟真实供应商成功、自动采纳/发布用户业务规则。

真实外站与模型测试已请求确认：练习站 quotes.toscrape.com、books.toscrape.com，沿用 glm-5.3-flash，最多六次模型请求，仅公开页面与虚构测试需求。未获得本次确认前不调用。现有 G2 的单次模型证据不代替 G4 完整编译验收。

## Checklist 与 Analysis

- P27：在真实 HTTP 与有鉴权 UI 完成新需求、字段发布、来源接入、候选审核、运行、数据与导出；服务端持久状态、固定版本及审计为权威。协议 fixture 的合成模型响应与真实供应商证据分开。
- P29/P32：用基准版本真实代码创建旧 SQLite/PG 实例，携带历史来源/规则/结果/密钥；升级后逐项核对不丢失历史绑定。回滚用升级前备份与匹配旧制品恢复到空目标，不原地降级数据库。禁止复用用户数据库。
- P30：1440x900、1280x800、1132x1028、1024x800，中英文、键盘/焦点、错误与刷新恢复；按旅程批量取证，不按字符串创建任务。
- P31：记录真实 72 小时状态、时间覆盖/空洞、调度分位数、Run/Delivery/签名和资源。当前窗口仅适用于冻结的 G3 运行版本；若修复影响实际观测路径或合同读取，明确保留旧证据及是否需要新窗口，不静默续用结论。
- 已知外部等待：72 小时最早 2026-09-13 20:26（Asia/Shanghai）结束，真实来源/模型授权及用户签收。先完成可执行项，缺失保持 Pending，不虚构 G4 全部完成或发布通过。

## 顺序与验证

1. 旧版本双数据库升级及匹配制品回退，对照历史数据、Artifact、密钥/凭据、绑定与审计。
2. 独立实例真实 API/Worker/HTTP 来源与接收器的端到端旅程，再以浏览器验证同一产品入口和错误恢复。
3. 获批的真实外站/模型小样本，限制调用次数，保存用量/耗时/失败与范围；不绕过失败扩大重试。
4. 四桌面中英文与键盘/焦点 QA、全套回归、候选制品摘要和运行手册复核；观测分析未齐时只交候选与剩余门槛。

行为修复先反例 RED，再最小实现与 GREEN。既有能力验证不伪造 RED。命令为独立 `EXTRIO_TEST_DATABASE_URL` 的 pytest、真实子进程/HTTP/CLI、`pnpm --dir web test --maxWorkers=1`、lint/build/api:generate、docset 和 diff 检查。新制品/测试进程使用独立端口和目录，结束清理仅限自己的临时资源。

## 退出

P27–P32 各项必须有真实证据；P0/P1 为零，P2 需明确处置或用户接受；72 小时和真实来源/模型缺失不标记通过。工程 Needs_Review 与产品 Accepted 分开。发布、部署、提交和正式签收仍需用户授权。
