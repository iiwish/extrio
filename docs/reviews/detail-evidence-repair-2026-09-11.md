# 详情正文证据修复

状态：Needs_Review。范围为模型网关的详情页证据裁剪、harvest 中同名字段的显式阶段绑定及对应回归测试。不修改已发布输出合同、来源授权、发布门或生产数据。采用局部直接修复，保留工作区既有修改，不委派。

## 诊断

目标来源 `collector_ggzyfw_beijing_gov_cn_f4d99adb` 的两次修复已成功抓取 3 个详情样本。`op_x_91244ead63c042/detail-002.html` 包含 25170 个字符，正文位于 `.div-article2`。原有 14000 字符模型证据先拼接重复链接与导航，再拼接文档，在正文前截断；模型没有获得真实正文容器。运行链路支持列表 URL 到详情文档的两阶段交接，本次问题位于模型证据，不是详情请求缺失。

## 实现

- 列表保持重复记录证据；生成与修复的详情样本使用独立分支，按非链接文本得分选取正文区域，保留实际祖先标签和属性。
- 单个过长文本节点缩短并明确标记，为后续字段保留空间；正文和文档上下文共享原有长度上限，不增加网络请求或模型预算。
- 详情保留有界的 head title/meta 区域，供模型选择准确的标题、日期等元数据，避免把标题容器的附属编号作为标题。
- 原始 Artifact、选择器执行、字段值和审核门均保持完整页面语义，没有用占位值或风险接受消除警告。
- 同名字段的明确阶段绑定用于字段定义、审核样本与运行输出。`detailUrl=list.detailUrl` 使用实际列表 URL，详情同名字段不能将其覆盖为站点根域名。无显式同名绑定时保留原有 detail 优先行为；不把语义角色别名添加为新输出字段。

## 验证

- TDD：导航挤占正文与长段落挤占后续表格两个反例先失败，实现后通过。
- 追加生成与修复调用链测试，确认两条路径均使用详情专用证据、列表仍使用列表证据。
- 已保存的三个真实详情样本均能在新证据中保留正文容器；完整 DOM 的正文长度分别为 258、464、395 字符。
- 最终模型网关、修复、探索、来源样本、运行与 Worker 回归 94 passed（23.43 秒）；测试文件 Ruff 通过。模型网关存在 4 处本次修改范围外的既有 E501 长提示词行，排除 E501 后两文件 Ruff 通过。
- 无活动任务时重新加载本地 API 和 Worker，保留原前端进程与来源配置；不自动发布规则。
- 真实重试 `op_x_0450924ad0af4f` 已提取正文，但确定性样本检查拒绝了“列表标题与详情标题不一致”：模型选中 `.div-title`，包含其子元素交易项目编号。没有放宽校验或提交失败候选。补充 head 元数据证据后，使用真实 ArticleTitle 元数据诊断再次修复。

## 最终结果

- `op_x_718322d6fa8044` 在第三个详情连接时出现 `source_connection_failed`，未进入模型编译；随后重试完成。
- `op_c537c3a3484248c8` 的正文与标题已通过，但人工检查发现三个预览明细 URL 被详情 SiteDomain 覆盖、entityKey 相同。规则明确绑定 `detailUrl=list.detailUrl`，旧合并代码未遵守该绑定。该候选没有发布或写入生产 Item。
- 阶段合并反例在修复前 3 failed / 2 passed，修复后全部通过；覆盖两个不同明细 URL 的实体标识隔离、审核选择器与样本、缺失列表值不回退、显式 detail 及无绑定时的兼容行为。阶段合并、运行、修复与冻结合同套件 64 passed。
- 追加显式 detail 绑定缺失的反例，确认审核样本不借用列表值掩盖缺失；先 RED 后 GREEN。该规则与运行输出的缺失处理一致。
- 最终完整相关链路回归 138 passed（38.69 秒），覆盖阶段交接、模型网关、修复、探索、来源样本、运行、Worker 和冻结合同。两份测试文件 Ruff 通过，`git diff --check` 与文档 manifest 校验通过。未修改前端文件，未重跑前端构建或布局验收。
- 全后端测试 476 passed / 20 skipped / 1 environment error（66.83 秒）。唯一错误为 PostgreSQL 备份测试的默认 127.0.0.1:5433 凭据认证失败；未修改该实例或凭据。不能据此宣称 PostgreSQL 全套通过。
- 最终真实操作 `op_x_338c367710f740` / `ai_run_a3abc75dc20945108dd6b069819477a1` 成功。三个样本均 accepted，正文分别为 269、475、406 字符，三个输出明细 URL 均等于对应 sourceUrl，三个 entityKey 各不相同，候选 warningChecks=0。
- 浏览器审核页显示真实正文和具体明细 URL，发布按钮可用；没有点击发布，没有接受风险或排除字段，activeRuleVersionId 为空，来源保持 ready_review。
- 旧正文 label 仍含模型写入的“样本截断”历史措辞，修复保留该字段语义元数据；实际 warning 为 null，以样本内容及验证结果为准。页面“风险已处置”说明属于既有通用成功文案，本次没有用风险接受替代提取。
