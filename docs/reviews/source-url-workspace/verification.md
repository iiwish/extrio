# 来源 URL 工作区验收

日期：2026-09-14。范围：SU-01 来源详情 URL 优先布局；不代表全部 P2/P3 已实现。结论：技术验收通过，待用户体验复核。

## 命令与回归

- RED：定向测试 3 failed / 13 passed，覆盖默认 config、完整 URL 配置区域和复制入口。
- GREEN：`cd web && pnpm test`，39 files / 240 tests passed。首次扩展回归有一个测试未计入审核 Tab 的风险数徽标，修正可访问名称匹配后全量通过。测试环境有两条既有 jsdom 跨文档导航提示，不影响测试结果。
- `pnpm build`：TypeScript 与 Vite 构建通过；`pnpm lint`：通过；`git diff --check`：通过。
- 中英文 collector-detail 递归键校验：各 405 个键，无缺失。
- 新增覆盖：普通/待审核默认分区、URL 保真与顺序、复制成功/失败、修改 URL 风险提示、取消无请求、仅保存携带 revision 且不触发生成、无候选时规则深链接回落配置且证据导出仍可达。
- 保留覆盖：错误后草稿与幂等键、pending 禁止关闭、只读与归档限制、显式运行视图、真实运行计数、Webhook 与投递操作。

## 桌面验证

目标为现有 localhost:5173 的真实来源 `collector_ggzyfw_beijing_gov_cn_53130add`。没有创建另一份原型，没有生成图片，没有提交保存、触发运行或改变调度。

| 视口 | 结果 | 截图 |
| --- | --- | --- |
| 1440x900 | URL 主位，计划与范围并排，无横向溢出 | [首屏](config-1440x900.png) |
| 1280x800 | 控件和文本无遮挡，无横向溢出 | [首屏](config-1280x800.png) |
| 1132x1028 | 计划与范围纵向排列，无横向溢出 | [首屏](config-1132x1028.png) |
| 1024x800 | URL 和编辑可见，下方操作滚动可达 | [首屏](config-1024x800.png)、[下方区域](config-lower-1024x800.png) |

四视口 document.scrollWidth 等于 viewport.width。打开编辑后 URL 为首个输入；修改草稿显示关闭调度、清除当前规则和检查点的提示，取消恢复原 URL。规则技术操作位于「规则与证据」可展开工作区。投递配置默认折叠，展开读取真实空状态。运行视图保留 200 接收 / 0 拒绝、手动运行、规则 50 页与策略 20 页的独立事实。

交互截图：[URL 风险提示](url-edit-warning.png)、[规则展开](rules-expanded.png)、[运行与结果](runs-results.png)。

## 设计复核

设计者自评：Pass with concerns，24/28，平均 1.71。产品适配、首屏任务、设计主张、URL 主位、层级密度、字体、桌面布局、真实内容、合同一致性和语义配色各 2；视觉表达、交互精细度、状态视觉覆盖、动效各 1。优势是来源入口和配置动作直接可见，低频技术内容不占首屏；不是全站视觉成熟度或目标用户无协助测试结论。

边界：未进行真实保存/生成/运行写入测试、英文浏览器视觉验收、200% 缩放或屏幕阅读器验收。长 URL 的精确保留与动作由组件测试证明，未向真实实例写入极端长 URL。运行锁定没有改动，本轮未启动运行验证。移动端不在项目范围。样本证据版本关联、需求内查询与部分失败重排属于独立后续工作。
