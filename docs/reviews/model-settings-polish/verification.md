# 模型设置验收

- 默认模型工具栏、供应商 DetailPanel、平铺模型行使用统一视觉；供应商身份与状态分层，数量明确显示，模型默认展开。
- 真实浏览器检查主页面、添加供应商与添加模型弹窗。取消后返回触发按钮；没有提交配置、读取密钥或发起模型调用。
- 校正浏览器缩放后的实际 CSS 视口 1440x900、1280x800、1132x1028、1024x800 均无页面横向溢出。截图检查使用默认桌面视口，各目标尺寸完成 DOM 几何检查。
- `pnpm build`、`pnpm lint`、`git diff --check` 通过。
- 模型设置与设置路由共 9 项测试通过，包括默认模型保存、凭据输入隐藏和供应商保存失败在弹窗内展示。新增用例显式清理 DOM，避免测试之间残留弹窗。
- 视觉自评：层级、间距、操作一致性符合详情页约定；实际数据仅一个供应商，未在真实环境创建多供应商或长名称测试数据。
