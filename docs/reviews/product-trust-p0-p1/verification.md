# P0 / P1 交付验证

日期：2026-09-14。结论：P0 基线和范围确认、P1 三项工程任务完成；用户产品验收待进行。范围见 [执行计划](../../planning/product-trust-p0-p1.md)。本报告不是全产品成熟度认证。

## 交付范围

- PT-01：首页和来源列表共用当前待处理分类。首次运行、最新异常、待审核及迁移等按同一口径判定；归档和进行中来源不误报；缺失运行记录不推断为首次运行。
- PT-02：需求默认页显示目标字段版本和活动来源对齐摘要，未知不算对齐；来源概览显示真实调度状态，规则分页上限与运行策略分别展示。入口保留上下文及键盘焦点，不写配置。
- PT-03：运行成功不等于范围完整。拒绝记录和未获取页面独立计数、独立入口；拒绝筛选写入 URL，可刷新恢复；网络/通用失败进入执行诊断，不默认要求修改规则。
- 四份产品文档已同步。现有前端入口、技术栈、依赖、后端和数据库不变。

## 自动化证据

命令工作目录为 `web/`，除特别说明外退出码均为 0。

| 检查 | 结果 |
| --- | --- |
| 初始完整测试基线 | 38 个文件、209 项通过 |
| PT-01 RED | 新分类模块缺失、首次运行来源未进入待处理，按预期失败 |
| PT-01 GREEN：`pnpm exec vitest run src/features/collectors/collector-attention.test.ts src/features/collectors/collectors-page.test.tsx src/features/home/home-page.test.tsx` | 3 个文件、24 项通过 |
| PT-02 RED | 对齐摘要及概览调度状态缺失，4 项失败 |
| PT-03 RED | 分组数量缺失、异常指向规则，2 项失败 |
| 最终 `pnpm test` | 39 个文件、232 项通过，48.96 秒 |
| 最终 `pnpm build` | TypeScript 与 Vite 构建通过，2079 个模块 |
| 最终 `pnpm lint` | 通过 |
| `git diff --check`（仓库根目录） | 通过 |
| 中英文键校验 | 对 11 个命名空间递归比较、归一化复数后缀，缺失键 0 |

测试覆盖只读角色、未知/归档对齐、手动及自动调度、中英文状态、页数上限与未获取并存、拒绝记录未完整返回、URL 恢复和分区焦点。回归过程中修复了分区焦点回跳、测试规则分页类型不完整、独立英文 i18n 实例污染后续测试的问题；最终完整测试重新通过，而非仅重跑失败项。

## 真实实例走查

使用已有 `http://127.0.0.1:5173` 实例，只读访问真实数据；未运行、发布、迁移、启用调度或调用付费 AI。

| 路径 | 观察结果 | 截图 |
| --- | --- | --- |
| 首页到来源待处理 | 首页 4 个来源需要关注，列表需处理 4 项，包含首次待运行来源 | `home-1440.png`、`attention-1440.png` |
| 需求默认字段页 | 标讯测试显示目标 v2、1 个活动来源未对齐；键盘进入来源分区且焦点正确 | `alignment-1440.png` |
| 来源概览 | 仅手动运行可见；规则最多 50 页与策略 20 页/300 条/回看 3 天分列；配置入口有效 | `source-1440.png` |
| 部分成功运行 | 2 条接收、9 条拒绝、8 个未获取页面；拒绝入口展示本次 9 条，刷新保留筛选；诊断入口进入执行过程 | `partial-1440.png` |
| 达到页数上限 | 200 条保存、20 页、0 拒绝，明确范围尚未确认全覆盖，不误导修规则 | `page-limit-1440.png` |

对需求对齐、来源概览、部分成功结果三个改动面分别验证 1440x900、1280x800、1132x1028、1024x800。12 组均无文档横向溢出；截图为 `alignment-*`、`source-*`、`partial-*`，尺寸读数见 [desktop-metrics.json](desktop-metrics.json)。共 15 张截图，临时视口覆盖已恢复。

## 边界与后续

- 浏览器证据为中文；英文使用隔离组件测试与键校验，不声称英文实机走查或完整屏幕阅读器验证。
- 对齐摘要采用需求详情现有绑定数据。尚未发起迁移的版本差异不进入全局待处理，因为列表 API 不包含完整目标契约；全局零待处理不代表全部需求已经对齐。
- 拒绝筛选只展示 API 当前返回的本次记录；条数不足时明确提示，不伪装全量，也不新增分页或重试能力。
- 自动运行仅展示已有计划事实，不承诺新鲜度 SLA。P2 及以后数据工作区、恢复执行能力和开源增长工作不在本次范围。
- 未部署、未修改后端执行语义，未验证真实新增采集/付费 AI/数据迁移/导出完整链路；保留用户已有 UX 审查目录。
