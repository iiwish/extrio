# CL-04 回归执行包与交付证据

状态：Needs_Review。依赖 CL-01/02/03 已实现，整份计划已由用户批准；顺序直接执行，不委派，不部署。不是用户 Accepted。

## 边界与验收

基于 `docs/planning/collector-lifecycle-plan.md` 和 CL-01/02/03 的实现继续回归。文档边界为 SSOT、product-contract、frontend-prototype、v0.2-acceptance、文档清单与本目录。浏览器脚本作为本目录验收附件，复用 scripts/g4-qa-instance.py；若验证发现缺陷，允许回到对应任务文件做最小修复并补充反例，不覆盖其他在途修改。

SQLite 和新建隔离 PostgreSQL 测试库运行完整后端测试；前端全量 test、build、lint 和生成类型检查。隔离 API/Worker 仅使用临时数据库、局部 HTTPS 合成协议模型与 localhost 来源，不调用付费模型、不触碰用户来源或历史数据。

浏览器覆盖需求内新增、基本信息编辑、空白删除、归档、恢复、跨需求调整、活动任务阻断、失败重试、保留历史归属与重建规则。以 1440x900、1280x800、1132x1028、1024x800 检查列表、菜单、详情和差异弹窗；关键状态检查中文/英文，不测试移动端。

执行前已核对工作区与依赖；测试数据和进程必须可识别，测试完成清理自身临时 PostgreSQL 容器，不触碰其他数据库。

## 验证结果

- 前端：`pnpm test` 37 文件 / 190 passed；`pnpm api:generate`、`pnpm build`、`pnpm lint` 通过。类型从 OpenAPI 生成，无手工维护的副本。
- 后端完整 SQLite + 隔离 PostgreSQL 最终回归：554 passed，耗时 141.27 秒；包含概览归档计数反例。用于验证的独立 PostgreSQL 容器已停止并自动删除，未触碰其他数据库。
- `uv run python ../scripts/update-docset-manifest.py --check` 通过；SSOT、产品合同、前端合同、发布验收及 API 合同一致。来源独立生命周期、revision、空白判定、历史归属和实体分区语义均有明确合同。
- 浏览器：复用独立 API 8049、前端 5199、临时 SQLite 与本地 HTTPS 合成协议模型。完成需求内新增、名称编辑、空来源删除并返回原筛选列表、历史删除阻断、归档与恢复、跨需求确认、重新探索与人工发布、新旧运行归属核对。
- 服务端已提交但响应丢失时，前端保留弹窗并复用同一 Idempotency-Key 重试，真实回执重放成功；暂停本次隔离 Worker 后的真实排队任务阻断归档。旧 Run 与 Item 的完整读取结果在调整前后相等，新 Run 使用新规则及目标需求归属。
- 中英文四个桌面视口共 38 个页面/弹窗状态：无页面横向溢出、弹窗均在视口内、列表表头与数据列坐标一致，pageErrors 为 0。额外检查 1024x800 下约 9800 字符来源说明编辑，弹窗 620x568，输入和操作可达。

## 回归修复

1. 概览的可执行发布来源计数排除已归档来源；RED 计数仍为 1，修复后两库通过。总来源数保留历史来源。
2. 来源行增加独立管理按钮后，表头遗留的 1000px 最小宽度造成小桌面列错位。表头与行统一 768px 下限，最近运行列扩到 144px，并添加浏览器列坐标断言后重跑完整旅程通过。
3. 基本信息编辑的长说明限制 textarea 高度并保留内部滚动，弹窗限制在视口内。
4. 验收脚本不等待活跃轮询页面的 networkidle；改用字体加载及显式状态断言，不改变真实 API 结果。

## 证据与审查

完整浏览器结果：[collector-lifecycle-browser.json](collector-lifecycle-browser.json)。代表截图：[英文最小桌面列表](source-list-en-1024.png)、[归属差异确认](reassignment-zh-1024.png)、[历史删除阻断](delete-blocked-zh-1440.png)。完整 39 张截图保留在本机 `/tmp/g4-qa-collector-lifecycle-20260911/lifecycle-screenshots/`，登录凭据与浏览器会话未复制进仓库。

合同自审覆盖：事务内阻断、来源墓碑、实际队列/租约保护、固定目标和历史归属、角色边界、幂等重试、缓存刷新。视觉自审 Standard Pass，适用项 24/24，平均 2.0；操作型密度、层级、字体、状态、键盘焦点和合同一致性通过，表现型 hero 与媒体不适用。移动端不在范围内。

余留边界：浏览器主流程使用 SQLite，PostgreSQL 由隔离测试覆盖；合成模型证明流程而非真实模型效果。不部署、不修改用户现有来源、不重启 8000/5173 现有实例；其后端重启时才加载新 API 与迁移。未创建提交，保留工作区既有改动。
