# D1-W1 验证与交接

日期：2026-09-06。状态：Engineering Complete / Product Review Pending。

运行页面：http://127.0.0.1:5173/collections。现有 Vite 与本地 API 服务继续使用，鉴权保持启用；真实浏览器使用已有管理员会话。本包不修改后端、账号、需求、来源、字段或运行数据，不调用外部站点和模型，不提交 Git。

## 实施结果

- 需求名称与两行目标摘要集中为同一业务单元，减少跨列阅读；技术标识保留为合同引用，不标为已冻结字段版本。
- 来源列同时表达已发布数、总数及待发布状态，不把规则发布覆盖解读为运行健康或调度启用。
- 使用中、已归档和全部需求的计数直接可见；搜索按名称/目标匹配，独立清除且恢复输入焦点，不重置状态或排序。
- 更新时间列默认降序，支持升序；同时间按稳定 ID 排序。搜索、状态和排序写入 URL，详情顶部返回及浏览器刷新保留三者。
- 刷新按钮具备 tooltip、忙碌反馈和重复请求禁用；加载/失败不报零，空列表、状态为空、搜索无匹配和请求失败分别处理。
- 保留独立需求 CRUD、角色权限与唯一正式前端；查看者没有新建入口。

## 自动化

| 验证 | 结果 |
| --- | --- |
| 初始需求列表 TDD RED | 3 项既有测试通过，7 项新增行为测试失败，确认当前页面缺少目标行为 |
| 列表 GREEN | 7 项新增行为通过；另加英文与长内容回归，列表共 11 项 |
| 详情返回回归 | 搜索、全部状态和升序一同往返；测试暴露 AppShell 未透传 sort，补齐白名单后通过 |
| `pnpm --dir web test --maxWorkers=1` | 29 个文件、125 项全部通过，48.02 秒 |
| `pnpm --dir web lint` | 通过 |
| `pnpm --dir web build` | TypeScript 与 Vite 生产构建通过 |
| `uv run --project backend python scripts/update-docset-manifest.py --check` | 通过，四份合同 hash 同步 |
| `git diff --check` | 通过 |

组件测试覆盖初始 loading、失败/重试、空数据、归档空状态、无匹配、清空焦点、组合 URL、排序与同时间稳定性、刷新期间保留数据、查看者权限，以及英文控件、长中英文原始目标和长合同 ID 的完整保留。既有新建/编辑/归档/恢复/删除、revision 冲突与字段编辑测试包含在全量回归中。

测试环境有两条既有 jsdom `Not implemented: navigation to another Document` 提示，测试均通过。构建发现新增测试误用了 Testing Library role 查询的 `exact` 选项，已修正；英文 title 断言使用不折叠空白的 normalizer，验证的是完整原始文本而非截断摘要。未跳过测试或放宽超时。本包不涉及后端实现，未重跑后端套件。

## 真实浏览器

浏览器：Codex In-app Browser，正式前端连接真实 API，已有 4 个使用中需求，0 个已归档需求。通过 Playwright DOM 与原生渲染截图核对；所有截图已保存、查看并核验像素尺寸。

| 视口 | 表格宽度 | 页面横向溢出 | 证据 |
| --- | --- | --- | --- |
| 1440x900 | 1184px | 无 | `04-list-after-1440.png` |
| 1280x800 | 1024px | 无 | `05-list-after-1280.png` |
| 1132x1028 | 892px | 无 | `06-list-after-1132.png` |
| 1024x800 | 784px | 无 | `07-list-after-1024.png` |

同视口对照 `01-list-before-1440.png` 与 `04-list-after-1440.png`：业务名称和目标归组，重复来源总数列合并为发布覆盖，新增更新时间；来源发布语义没有扩展为健康结论。四种桌面视口下操作完整可达，长目标限制两行；1024px 下合同标识在自身列换行，不遮挡相邻日期或操作。没有全站 CSS 或移动端重构。

真实操作证据：

1. 选择已归档显示“暂无已归档需求”，不是“没有匹配”；`08-archived-empty-1024.png`。
2. 在已归档范围搜索不存在的词，出现独立无匹配状态；Tab 到清空按钮可见焦点与 tooltip，Enter 清空后焦点回到搜索框，仍选中归档；`09-search-keyboard-1024.png`。
3. 选择全部、搜索“全国”、用 Enter 切换更新时间升序，URL 包含 `status=all`、`q=全国` 与 `sort=updated_asc`。进入全国需求详情，顶部返回链接包含三个参数。
4. 从详情返回、点击列表刷新、执行浏览器 reload，仍显示 1 条结果、全部选中、升序和原关键词；`10-return-state-1024.png`。
5. 最终恢复默认需求列表，浏览器 error/warn 日志为空，临时视口覆盖已清除，保留可操作页供用户复核。

英文与超长合成内容在隔离组件测试中验证，未切换用户语言偏好，未声称已完成英文浏览器视觉验收。失败/慢请求/查看者在组件测试中覆盖，未通过中断本地服务或修改真实账号制造这些状态。真实运行规模仅为现有 4 个需求，不代表大规模性能已验收。

## D1-W2 交接

- 需求详情 `02-detail-before-1440.png` 和关联来源 `03-detail-sources-before-1440.png` 是真实走查依据，不是本包重设计成果。
- 下一工作包保持业务身份、状态、主动作、目标、字段/来源任务视图的清楚层级；技术合同按需阅读，不与字段草稿混同。
- 已证实缺陷：选中关联来源后 reload 回到采集字段，当前分区不进入 URL。D1-W2 处理分区恢复，并保留本包的列表 query 上下文。
- 字段编辑、草稿与来源差异、错误/冲突保留、添加来源与返回路径是详情包的联合验收范围。未实现的字段版本发布、来源迁移、AI 字段建议不报告成功。
- 本包完成第一个开发日的 1/2 工作包，不代表整个开发日或 1.0 已完成；页面方案与产品 Accepted 由用户复核。
