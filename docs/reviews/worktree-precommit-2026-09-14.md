# 工作区提交检查

## 范围

审查基线为 main 的 86ebcc4，覆盖检查开始时积累的后端、Web、合同、迁移、工具和交付证据。共享实现按后端/API、Web、文档三组提交，不拆分同一工作流的内部补丁。不推送远端、不发布服务或规则。

检查期间出现的 `docs/reviews/product-ux-2026-09-14/` 不属于本次范围，保持未提交。本地数据库、密钥、运行日志、构建产物受现有 ignore 规则保护，不纳入提交；保留仓库既有的验收截图管理方式。

## 检查与修正

- 后端 CI 的 Ruff 检查发现 211 项问题，主要是超长行、导入顺序和 pytest 注入 fixture 的静态误报。按现有规则整理，保留 fixture 注册；补充未使用版本结果的断言，将异步测试内的阻塞子进程创建移入线程。
- PostgreSQL fixture 默认连接本机 5433，且导入到其他测试模块后不继承模块跳过条件，导致完整备份用例认证失败。测试数据库仅接受显式 `EXTRIO_TEST_DATABASE_URL`，fixture 自身检查可用性，跳过信息不包含连接凭据。
- 重点追踪字段版本冻结、来源生命周期与历史归属、任务租约/取消、凭据恢复、来源网络边界、AI 验证反馈和页数终态。该检查不是对全部累计改动的独立安全审计，也不替代目标环境发布验收。

## 验证

- Web：38 个文件、209 项测试通过；lint、build 通过。
- 后端：Ruff 通过；wheel 构建通过，包内包含 OpenAPI 合同、分页解析器和 010 迁移。
- OpenAPI 重新生成的 TypeScript 与工作区文件逐字一致；文档清单校验通过。
- 常见私钥与访问令牌模式扫描未命中；暂存路径检查未发现数据库、密钥、环境文件或运行目录。此扫描不替代 CI 的 Trivy secret scanner。
- SQLite/PostgreSQL 全量测试使用本机临时 PostgreSQL 16 容器和随机回环端口，与其他项目数据库隔离：671 passed，293.36 秒。补充测试数据库显式配置回归后，隔离与完整备份组合另跑 10 passed、1 skipped（未配置 PostgreSQL 时的预期跳过）。

本次不重新调用外部模型，不重跑真实来源采集，不执行 maco 或生产环境部署。
