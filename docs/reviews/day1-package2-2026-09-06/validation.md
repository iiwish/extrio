# D1-W2 验证与交接

日期：2026-09-06。状态：Engineering Complete / Product Review Pending。

运行地址：http://127.0.0.1:5173/collections/collection_nationwide_tender。

## 结果

需求详情沿用正式 AppShell、AuthGate、QueryClient 与 API，业务身份、使用/归档状态、来源发布覆盖与更新时间集中在头部。多段或长目标可展开全文；字段说明归入名称/标识所在单元，类型、约束、覆盖和编辑操作按稳定列比较。编辑操作列宽 84px，保存/取消/添加位于顶部操作条，长表编辑时 sticky。关联来源把名称与入口网址归组，不可用或未知合同显示“无法确认”，不显示假零。

`section=sources` 控制当前分区，缺省或无效值显示字段。浏览器刷新恢复分区，列表返回只携带 q/status/sort。切换分区保留本地字段草稿，标签显示未保存；跨页面离开经 React Router blocker 确认，浏览器刷新/关闭保留 beforeunload 提醒。提交期间离开确认按钮禁用，不把正在执行的保存误当成可取消操作。后台读取失败保留页面与本地编辑；基本信息/字段冲突保持输入，放弃重载动作明确。

保留字段覆盖差异、原始合同只读、字段草稿与执行规则的边界。不新增字段发布/迁移/AI 建议，不更换路由器或新建原型，不改后端或用户业务数据，不提交 Git。新建需求表单只走查与回归，不增加创建行为。

## 代码边界

- `web/src/features/collections/collection-page.tsx`：布局、分区 URL、导航保护、后台错误保留、基本信息保存反馈与弹层焦点。
- `web/src/features/collections/collection-fields.tsx`：编辑状态通知、顶部操作条、稳定 colgroup、说明归组、字段/证据弹层焦点。
- 三个相关测试文件：详情/CRUD 使用与正式路由同类的数据 Router，字段组件仍可独立测试。
- `web/src/index.css`：仅需求详情、字段和关联来源局部样式；不改 D1-W1 列表或全站设计。
- 中英 `common.json`、四份合同、文档 manifest、两份规划与本目录。

## 自动化验证

| 命令或检查 | 结果 |
| --- | --- |
| 详情 TDD RED | 2 项既有测试通过，4 项新增目标行为失败 |
| `pnpm --dir web test --maxWorkers=1` | 29 个文件、134 项全部通过，60.83 秒 |
| `pnpm --dir web lint` | 通过 |
| `pnpm --dir web build` | TypeScript 与 Vite 生产构建通过 |
| `uv run --project backend python scripts/update-docset-manifest.py --check` | 通过 |
| `git diff --check` | 通过 |

9 项新增回归覆盖分区深链接/重挂载、跨分区草稿保留与离开确认、长目标全文、未知输出字段数、后台读取失败、英文与长业务名称、提交中离开限制、字段冲突和弹层焦点/卸载提醒清理。既有 CRUD 测试覆盖创建重试复用命令 key、失败保留基本信息、revision、归档/恢复、删除确认和查看者权限；D1-W1 列表的 11 项测试保持通过。

测试期间修正了 unavailable 合同 fixture 缺少必需 ruleVersion、说明归组后的可访问名称断言、QueryClient 批量通知的异步等待，以及英文实例对后续组件测试默认语言的污染。未跳过测试或提高超时。两条既有 jsdom Document navigation 提示保留，不影响通过。本包未重跑后端套件，后端不在变更范围。

## 真实浏览器证据

使用已有管理员会话，鉴权启用；真实全国需求含 5 个汇总字段与 3 个已发布来源。浏览器只执行读取、分区/目标切换、编辑模式、弹层打开/取消与新建表单取消，没有保存、创建、删除或归档真实记录。未访问外站或调用模型。

| 视口/状态 | 截图 | 检查 |
| --- | --- | --- |
| 1440x900 原页面/编辑 | `01-detail-before-1440.png`、`02-edit-before-1440.png` | 当前轮走查基线 |
| 1440x900 详情/编辑 | `03-detail-after-1440.png`、`04-edit-after-1440.png` | 身份层级、摘要、顶部保存和稳定编辑列 |
| 1280x800 详情 | `09-detail-after-1280.png` | 页面 scrollWidth 1280，无横向溢出 |
| 1132x1028 详情 | `10-detail-after-1132.png` | 页面 scrollWidth 1132，无横向溢出 |
| 1024x800 详情/编辑 | `13-detail-after-1024.png`、`05-edit-after-1024.png` | 页面 scrollWidth 1024；编辑列 230/110/188/172/84px，操作可达 |
| 1024x800 字段弹层/离开确认 | `06-field-dialog-1024.png`、`07-leave-confirm-1024.png` | 控件与确认完整可见，未遮挡关键操作 |
| 1024x800 关联来源 | `08-sources-after-1024.png` | 名称/网址归组，真实状态与字段数可比 |
| 1024x800 基本信息/新建表单 | `11-basic-edit-1024.png`、`12-new-requirement-1024.png` | 名称/目标可访问，空新建不能保存，取消可返回 |

全部 PNG 已本地保存、查看并通过 sips 核对精确像素尺寸。视口覆盖已恢复默认；最终详情保留为可操作交付，最终浏览器 error/warn 日志为空。

真实交互验证：目标展开后 `aria-expanded=true`，收起恢复摘要；字段编辑弹层取消后焦点恢复到“编辑字段 标题”；跨到来源页仍显示未保存，点击列表返回弹出离开确认，继续编辑不丢草稿。取消草稿后选择来源并 reload，仍选中来源。组合 q=全国/status=all/sort=updated_asc/section=sources 在详情刷新后保持，返回列表仅去除 section，仍为全国搜索与升序。基本信息用 Escape 关闭，等待退出动画后焦点恢复到“编辑基本信息”。

## 复核与限制

设计复核：Pass with concerns，24/26，平均 1.85（运营台不适用 Hero spectacle）。产品匹配、对象识别、信息组织、字段编辑操作、层级、排版、配色、四种桌面布局、真实内容、交互与合同各 2 分；差异检查仍使用既有模态证据，未扩展为并列对比，signature 1 分；英文/慢请求/冲突的视觉覆盖有限，state craft 1 分。该评分是工程自检，不是用户签收。

英文、超长合成名称、慢请求、冲突与权限在组件测试中验证，真实浏览器视觉证据为中文与现有数据，不能据此宣称全站/全语言或大规模性能验收完成。100 字段长表的 sticky 极端场景未做真实浏览器压力验证；本次浏览器验证为 5 字段。

第一个开发日 D1-W1 与 D1-W2 均已工程交付。下一工作包 D2-W1 检查来源接入流程、需求归属、批量输入/逐条错误和返回路径；来源详情/接入返回需求时的具体分区与编辑上下文联动在来源工作包中继续完善。字段版本发布、模板、AI 字段建议和来源迁移仍属于后续功能任务，不从 40 个功能基线任务直接扣除本轮体验工作量。产品 Accepted 待用户复核。
