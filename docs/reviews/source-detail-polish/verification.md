# 来源详情与字段提示验收

日期：2026-09-14。范围：需求字段提示文案与来源详情配置首屏；运行、规则分区保留既有流程，只回归页头和导航布局。

## 结果

1. 需求提示直接说明哪些来源尚未应用最新字段要求，以及来源不会自动更新。未知版本单独显示，不以 0 个已对齐替代。入口命名为“查看来源字段版本”，切换到全部关联来源，不暗示已筛选、不自动迁移。
2. 来源详情以完整入口 URL 为独立白色工具区，复制/打开/编辑入口保留。说明和需求归属并列；定时运行和采集范围在 1024px 及以上采用两列，投递入口在首屏内。
3. 来源覆盖表头和字段证据弹窗明确说明：字段存在不等于约束、版本一致。

## 桌面证据

- [1440x900 配置](config-1440.png)、[1280x800 配置](config-1280.png)、[1132x1028 配置](config-1132.png)、[1024x800 配置](config-1024.png)。所有截图已检查，文档宽度与视口一致，URL 未截断，投递入口底部不超过 696px。
- [1024px 需求提示](alignment-1024.png)、[运行分区](results-1024.png)、[规则分区](rule-1024.png)、[定义编辑](definition-1024.png)。编辑通过既有 `edit=definition` 深链接打开，未更改输入或提交。浏览器最终返回来源配置页并恢复原始视口。
- 本轮浏览器普通按钮激活未稳定生效，未据此宣称实机点击及取消验收通过；编辑深链接显示正常，分区键盘切换和 URL 状态恢复已观察到。编辑、取消和写入保护由现有自动测试覆盖，此交互需用户实机复核。

## 自动验证

- `pnpm test`：42 个文件、259 项通过。
- 最后的覆盖解释文案补充后，`pnpm exec vitest run src/features/collections/collection-page.test.tsx src/features/collections/collection-fields.test.tsx src/features/collectors/collector-context.ui.test.tsx`：51 项通过。
- `pnpm build`、`pnpm lint`、`git diff --check` 通过。
- 无后端变更，无真实配置写入、模型生成、采集、迁移或发布。

## 边界

来源页本轮主要打磨配置首屏，不将规则编辑器、审核深层工作区或运行结果视为已重新设计。真实浏览器样本为已发布来源，其他状态依赖自动测试；英文、屏幕阅读器与大规模真实数据未做专项视觉验收。内部 Standard 评审 22/24，平均 1.83/2，交互及状态的实机验证各保留 1 分关注项，不替代用户签收。
