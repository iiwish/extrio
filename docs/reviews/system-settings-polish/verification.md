# 系统设置验收

- 范围：系统分区的账户偏好、实例运行状态、采集策略、用户管理；模型分区不在本次范围。
- 设置相关 3 个测试文件、13 项测试通过，覆盖用户管理、采集策略及运行诊断。
- `pnpm build`、`pnpm lint` 通过。
- 真实本地浏览器确认四个统一白色面板、并列运行指标、采集策略开关与用户操作可见。未修改用户、凭据、语言或安全策略。
- 校正浏览器 90% 缩放后，实际 CSS 视口 1440x900、1280x800、1132x1028、1024x800 均无页面横向溢出。
- 默认视口截图可检查布局；切换视口后的截图存在浏览器捕获缩放与黑边问题，不作为逐尺寸像素验收证据。各尺寸已完成 DOM 几何检查。
