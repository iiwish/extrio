# AE-05 验证与评审

状态：Needs_Review。

## 实现

- 新任务总调用 16 次；发现阶段为编译及校验预留 4 次。输入、输出、时间上限不变。恢复沿用不高于历史记录的调用上限。
- 单节点读取上限 6000 字符，按实际序列化 token 估算分配；试算不计入已读覆盖。最多 6 个请求批量处理，目录展开最多 16 个子节点。
- 发现提示包含规范对象示例；归一化错误附带字段路径和原因，Schema 与 selector 失败反馈回模型。API 诊断仍不保存原因正文。
- 跨验证循环维护提案摘要与读取标识，连续三次无新证据或新提案停止，取消和预算失败仍不提交候选。

## 工程证据

- RED：新增测试 5 failed / 1 passed，分别确认旧版读取截断、8 次默认预算、重复读取、重复提案及缺失字段反馈问题。
- GREEN：生成/修复、证据、预算、Worker 恢复、阶段交接、冻结合同及浏览器失败相关测试 139 passed。
- 前端任务页与证据组件 8 passed；`pnpm build`、`pnpm lint`、相关 Python Ruff 检查通过。
- AI 任务中英文 key parity、文档 manifest 检查、`git diff --check` 通过。
- 中英文、四个桌面视口、两种停止原因共 16 项 Playwright 检查通过；无真实 API 写入。人工检查最小英文视口，错误说明可见且未溢出。
- 评审确认：阶段额度不足在发起调用前阻断；无进展异常不被 selector/模型重试捕获；恢复不提高历史额度；完整样本校验及人工发布边界未改变。小窗口批量读取回归发现过样本被挤出的问题，压缩重复协议措辞后通过原断言。

## 真实验证

来源：北京市公共资源交易工程建设招标计划列表。模型：现有 custom 供应商的 glm-5.3-flash，保守默认 32768 上下文。

一次独立验证实际调用 5 次：发现阶段 2 次展开、1 次提案；编译阶段 2 次提案，包含一次确定性校验后的纠错。三个详情样本均 accepted，最终 validated。输入 token 20423、输出 1386，总 21809，耗时 56.83 秒。

结果保存在 `round-efficiency-live.json`。候选仅保存于本机 `/tmp/extrio-round-efficiency-verification/candidate.json`，数据库零写入、现有采集器完整比较未变、没有发布。历史失败记录保持原状。

## 本地运行

确认无活动 Job、完成 SQLite 备份后，重启本项目 API 与 Worker，保留原 Vite 服务。`/readyz` 返回 `ready: true`；新代码已在 `http://127.0.0.1:5173` 对应本地实例生效。没有部署到远程服务器。

## 残余风险

本次真实来源内容有更新，不是同一快照的严格 A/B；单次成功不能证明 16 次对所有站点足够，也不能分别量化每项优化的收益。发现阶段预留的是调用次数，不保证剩余 token 或时间。token/time 仍可能先耗尽。此次没有重跑全仓全量测试，使用上述风险相关回归。
