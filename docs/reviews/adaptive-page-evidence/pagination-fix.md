# AE-08 分页识别与验证

状态：Needs_Review。实现与本地验证完成；真实供应商模型重试不在本次离线验收内。

next_link 使用普通 href 或整段匹配的静态 location.href 字符串赋值（可包装 encodeURI）；不执行 JavaScript，不解析变量、拼接或附加语句。静态目标只允许同源 HTTP(S)，空链接、片段、当前页不能充当下一页。发现和运行共用解析器，运行仍受现有来源网络策略与页数限制约束。

发现阶段验证下一页规则；存在可识别下一页时，不能以 none 或查询参数分页替代静态路径分页。模型收到从快照提取的候选 selector 与目标提示；失败以具体稳定分页错误码和字段路径留证，不记录任意页面/供应商错误正文。

允许文件：分页 helper、harvest、explorer、model_gateway、相关测试与证据；任务中英文诊断；四份产品文档。先 RED 后 GREEN，验证静态跳转白名单、恶意输入、普通链接兼容、发现纠正、完整真实快照离线探索与运行两页。无新增付费模型调用、不发布、不覆盖既有规则，不扩大网络权限。

## 验证证据

- 静态地址解析 12 个测试先失败、后通过；分页诊断 3 个测试通过。相关后端组合 103 passed；补充实际两页采集后 runtime/pagination 24 passed；安全、编译、模型网关组合 58 passed（组合包含重复测试，不合计）。
- `pagination-replay.json`：使用 op_af66f32a1fee4c71 的真实快照与 Worker 字段约束，确定性模型先返回原始错误分页，再接收发现反馈并纠正，完整探索验证通过；持续错误字段仍以 MODEL_NO_PROGRESS 停止。模型响应为测试替身，不是供应商调用验收。
- 前端两个相关测试文件 8 passed；pnpm build、pnpm lint 通过。`pagination-ui.json` 记录 3 种错误、2 种语言、4 个桌面视口共 24 次检查，零真实 API 写入。
- 本地 API、Worker 和 Vite 已重启加载；`/readyz` 返回 true。数据库备份位于 `/tmp/extrio-before-pagination-fix-20260914.db`，重启后 collectors/jobs 与备份一致；无发布、无新增模型调用。

动态脚本、变量和字符串拼接不属于静态分页支持范围。此次已证实并修复保存的分页规则失配；失败任务未保存各轮候选正文，不能把所有历史验证失败都归因于分页。
