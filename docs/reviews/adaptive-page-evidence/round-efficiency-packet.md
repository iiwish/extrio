# AE-05 多轮效率优化

状态：Needs_Review。用户已确认四项优化方案及 16 次调用的验证上限；直接执行，不委派。实施与验证见 round-efficiency-review.md。

## 合同与范围

总调用默认 16 次，发现阶段最多使用其中 12 次，为编译及验证纠错预留 4 次。输入 120000、输出 16000 token、240 秒安全上限不变。读取按实际上下文余量批量分配，单节点最多 6000 字符，展开最多 16 个子节点。连续 3 次无新增证据或新提案时停止。反馈包含稳定错误码、字段路径及确定性校验原因；网页内容和模型提案始终是不可信数据。

允许文件：后端 adaptive_compile、model_budget、page_evidence、model_gateway、explorer、worker 及相应测试；AI 任务中英错误文案；四份产品文档及本目录证据。保留已有工作区改动，不改来源网络权限、不自动发布、不修改既有规则和生产 Item。

## 执行与验收

1. RED：覆盖完整区域读取、批量小窗口、阶段预留、重复行为、具体校验反馈及总上限。
2. GREEN：实现后运行上述模块及生成/修复集成回归；前端 build、文案 parity 及任务诊断测试。
3. Review：检查预算失败不能提交、恢复不提高历史上限、诊断不泄漏正文或凭据。
4. 真实验证最多一次、16 次模型调用，独立候选输出，不发布；记录实际结果，不因失败追加调用。

检查：范围和用户批准齐备；已确认来源 DOM 可读；调用与 token 仍在应用侧强制预留；不引入数据库迁移。无未解决的执行前阻断项。完成后进入 Needs_Review，工程测试与真实效果分别报告。
