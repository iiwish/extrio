# AE-07 完整验证循环修复

状态：Needs_Review。用户要求继续检查并修复反复编译/验证问题，直接执行，不委派。

## 诊断与合同

最新任务 op_x_433fa9d739e941 已越过编译，在三个样本上持续产生 DETAIL_URL_MISMATCH。完整验证门把不属于业务输出合同的内部 detailUrl 当成 extractedData 必有字段。已绑定合同只输出 title、detail_url、content、publish_date，不能为满足内部检查额外注入 detailUrl。

内部 URL 交接从列表记录与实际详情样本核验；业务输出仅按自己的合同核验。无进展不仅看原始提案是否不同，还看同一证据下是否解决了验证问题；仅修改说明或重复未解决问题不能无限消耗余下预算。已验证候选仍需人工审核，不自动发布。

## 范围与验证

允许文件：explorer、adaptive_compile、相关后端测试、离线回放脚本、本记录及涉及无进展语义的四份产品文档和中英文任务停止说明。不改网络权限、数据合同、模型预算；不追加真实模型调用，不覆盖历史失败。

TDD：新增完整 explore 流程测试（真实编译与最终反馈门、模拟只读浏览器和模型），验证业务 URL 别名合同成功退出；负例保留真实交接缺失/错误拒绝；变动提案但相同验证失败的循环提前停止。对最新真实失败快照使用 Worker 等价上下文执行完整离线 explore，不只调用中间候选函数。

检查：已确认编译输出与最终循环校验是不同的验收边界。用户已要求修复，范围与证据明确，无需提高调用上限。保留工作区已有修改，完成后在队列空闲时重载本地服务并检查 readiness。

## 验证结果

- RED：加入最终反馈门后，两个业务别名合同测试失败；变动提案但错误未解决的停止测试失败。共 3 failed / 9 passed。
- GREEN：相关后端回归 119 passed；前端任务页及诊断 8 passed；构建、lint、相关 Ruff、git diff --check 通过。
- 最新失败任务的真实快照以完整 Worker 合同执行离线 explore：正确的确定性模型替身在发现和编译各一次后通过最终验证，三个样本 accepted；持续变动错误 selector 的替身在发现一次、编译四次后以 MODEL_NO_PROGRESS 失败，未产生可提交候选。两项结果见 validation-loop-replay.json。没有真实模型调用、网络请求或数据库写入。
- 中英文、四个桌面视口、两种停止原因共 16 项浏览器检查通过；最小中文视口人工检查无文字溢出。
- 真实 URL 交接缺失仍被拒绝；当 detailUrl 本身属于业务输出时，错误值仍被拒绝。没有通过删除质量门或向冻结输出偷偷添加字段来规避失败。

## 残余风险

本次使用模型替身验证完整应用控制流，不宣称真实供应商效果通过；不重写此前真实任务的失败历史。上次 AE-06 离线验证只覆盖了中间候选生成，遗漏最终 _sample_issues 门，本次测试和回放均进入完整 explore。未重跑全仓全量测试。
