# G1 验证与交接

日期：2026-09-06。状态：Engineering Delivered / User Review Pending。范围见 [工作包](packet.md)，产品适用范围见 [1.0 合同映射](../../planning/v1.0-scope-matrix.md)。工程完成不等于用户页面 Accepted，也不等于 1.0 发布验收通过。

## 交付结果

- P01：单组织自托管版本的 FR/AC/NFR 适用性、部署边界和退出条件已映射；SSOT、产品合同、正式前端合同及验收合同保持一致。字段版本、内置模板、真实 AI 建议、SQLite/PG 和恢复验证均保留为必达项。
- P19：新增有鉴权的 `GET /api/v1/overview`，服务端直接聚合全量数据，不使用列表的 200 条样本计算指标。IANA 时区决定自然日/周/月窗口；成功率以完成运行作分母，活动运行不计入；实体先取全量最新记录，再按持久化时间及当前决定计数。跨年、夏令时、秒/微秒边界、非法时区及双数据库有回归。OpenAPI、生成类型、API 客户端和首页一致。
- 页面：来源列表使用稳定比较列，详情采用紧凑分区与明确异常提示；概览突出真实指标和时间口径；运行、AI 任务、数据与设置收敛信息层级和操作反馈。1024px 下宽表局部滚动，批量接入删除列和底部操作可达；设置开关命中区域不再造成整页横向溢出。
- 交互：搜索清空不影响其他筛选，来源/运行/实体之间返回保留路径与查询，详情分区刷新可恢复；接入保留指定需求和失败后的输入，离开未保存编辑有确认；设置弹层关闭恢复触发按钮焦点，登录密码可显示/隐藏，退出失败保留会话并提供重试。
- 真实性与权限：首次读取错误不显示健康或假零；后台错误保留已加载内容；来源运行/配置等写操作遵守现有角色边界。HTML 实体正文采用惰性解析后的文本展示，源代码以转义文本查看，不挂载活动 HTML。Mock 环境不能冒用真实聚合结果。

## 自动化验证

以下为最终代码状态的完成结果，不以早期增量测试代替最终回归。

| 命令 | 结果 | 覆盖说明 |
| --- | --- | --- |
| `pnpm --dir web test --maxWorkers=1` | 151 passed，34 files，94.97s | 页面、权限、上下文、未保存保护、焦点、HTML、异步合同等 |
| `EXTRIO_TEST_DATABASE_URL=<isolated-pg16-url> uv run --project backend pytest backend/tests -q` | 262 passed，47.23s | SQLite 默认测试、PostgreSQL 16 专项、鉴权、OpenAPI Schema、全量聚合及现有备份恢复测试 |
| `pnpm --dir web lint` | exit 0 | 无最终 lint 错误 |
| `pnpm --dir web build` | exit 0 | TypeScript 与 Vite 生产构建通过 |
| `pnpm --dir web api:generate` | exit 0 | OpenAPI 生成前端类型 |
| `git diff --check` | exit 0 | 工作树差异格式检查 |
| `uv run --project backend python scripts/update-docset-manifest.py --check` | exit 0 | 文档清单与版本一致 |

下载单元测试中的 JSDOM navigation 提示不影响测试结果，真实浏览器另行核对导出。PostgreSQL 使用本次创建的隔离临时容器，不使用其他项目数据库；完成后容器已停止并移除。尝试 PostgreSQL 17 时发现本机 `pg_dump` 16 与服务端主版本不匹配，完整绿色结果以 PG16 为准，不宣称 PG17 备份工具链已验收。

## 真实浏览器证据

本地正式前端 `http://127.0.0.1:5173/`，真实有鉴权 API `http://127.0.0.1:8000/`，Playwright Chromium。使用用户批准的本地管理员登录，凭据未写入仓库。业务走查以读取为主，没有为了制造结果执行来源创建、发布、运行、删除、模型调用或 Webhook 发送。

- [四视口矩阵](browser-matrix.json)：16 个页面 x 1440x900、1280x800、1132x1028、1024x800，共 64 组。主内容非空、整页无横向溢出、无捕获的页面脚本错误；最终截图为 `after-*.png`。宽表自身横向滚动符合桌面合同。
- 来源列表筛选后进入详情再返回，保留 `q=zycg&view=attention`。运行列表筛选后进入运行、实体，再逐级返回，保留 `status=attention&q=zycg`。实体质量分区刷新恢复 `section=quality`。
- 数据查询保留来源、采集器和质量筛选；清空搜索不清空其他筛选。真实过滤 JSONL 导出得到 1 条符合来源、rejected 与关键词的记录；全量导出在页面只加载 50 条时得到 158 个不同实体，没有按已加载行数截断。
- 批量接入选择指定的全国公共资源交易标讯需求；有效/无效 URL 预览给出 2 条、1 有效、1 无效；删除无效行、继续编辑、放弃离开行为经过浏览器检查。创建失败保留输入及重试成功由隔离 UI 测试验证，没有真实写入来源。
- 规则编辑器查看 JSON 后 Escape 关闭；模型供应商和用户弹层关闭后恢复原按钮焦点。登录四桌面视口、密码可见性和键盘可达性补查通过。
- 8 个代表性页面在英文 1024px 下补查。历史诊断和采集内容保留来源语言，不把后端诊断当作可随意翻译的应用文案。
- 概览 503 使用浏览器拦截合成故障：呈现不可用与占位符而非假零，解除拦截并刷新恢复真实 7/7 来源统计。[合成错误截图](synthetic-overview-error-1024.png)不作为真实服务故障证据。

代表性视觉证据：[概览 1440](after-overview-1440.png)、[概览 1024](after-overview-1024.png)、[来源列表](after-sources-1440.png)、[来源详情](after-source-detail-1024.png)、[运行详情](after-run-detail-1440.png)、[实体详情](after-item-detail-1440.png)、[接入错误与操作](onboarding-validation-1024.png)、[未保存保护](onboarding-unsaved-1024.png)、[设置](after-settings-1024.png)、[英文运行详情](en-run-detail-1024.png)、[登录键盘](login-keyboard-1024.png)。

## 交接与后续边界

目标起始时间为 2026-09-06 17:22:12（Asia/Shanghai），含实现、回归、浏览器复核与交接的墙钟时间约 75 分钟，以目标工具最终记录为准。没有等待用户或外部授权；本地工具/测试等待包含在墙钟内，未单独可靠拆分。没有为凑足 3 小时追加无关范围。修改保留在当前工作树，未创建提交，既有用户修改未回退。

前端开发服务与真实 API 保持可访问；API 已恢复为独立后台进程，登录会话重启后仍有效，概览接口复查为 200。既有 Worker、模型配置、调度和业务数据未因本包验收而改动。

- G1 无已知未处置的阻断缺陷；页面风格与操作体验仍待用户复核，不能把自动化绿色等同于设计签收。
- G2 负责字段版本、来源绑定与迁移、内置模板、真实 AI 字段建议及对应界面；本包不声明这些能力完成。
- 概览主指标是全量聚合，待关注区域仍是已加载来源/运行的有限投影，不是全量告警计数。来源/运行列表现有 50/200 条边界和规模性能归后续规模与可靠性验证，不用本次导出证明容量达标。
- 本包不执行真实发布到运行到交付的写入旅程、不重建初始化实例、不做生产灾备、故障注入或 72 小时观测。现有备份测试绿色不替代 G3/G4 的完整恢复验收。
- 浏览器证据仅 Chromium；不是跨浏览器兼容结论或完整 WCAG 审计。HTML 阅读模式优先安全与可读性，不承诺原页面排版复刻。
- 剩余 G2–G4 暂按 9 小时执行目标组织，补充包按实际缺口启用；G2 实测后再校准模型/迁移类任务，不能按 G1 的页面吞吐同比例压缩。
