# Defaults to /api/v1 when omitted. Local full-stack development uses the FastAPI URL below.
VITE_API_BASE_URL=/api/v1

# Mocks are opt-in. Keep false for the full local stack.
VITE_ENABLE_MOCKS=false
